# Database (required for auth and saved items)
DATABASE_URL="postgresql://user:password@localhost:5432/your_db"

# Better Auth – base URL for callbacks/redirects (e.g. login/signup)
# Local: http://localhost:3002
# Production: https://your-app.vercel.app
BETTER_AUTH_BASE_URL="http://localhost:3002"

# App URL (client-side; used by auth client for API base URL)
# Optional: defaults to current origin in browser, or http://localhost:3002 in SSR
VITE_APP_URL="http://localhost:3002"

# Firecrawl – required for scrape/import features
# Get a key at https://firecrawl.dev
FIRECRAWL_API_KEY="fc-..."

# OpenRouter – for AI summary and other AI features
# Get a key at https://openrouter.ai
AI_KEY="sk-or-..."
