# Database (required for auth and saved items)
DATABASE_URL="postgresql://user:password@localhost:5432/your_db"

# Better Auth – base URL for callbacks/redirects (server-side; required on Vercel)
# Local: http://localhost:3002
# Production: https://your-app.vercel.app (no trailing slash)
BETTER_AUTH_BASE_URL="http://localhost:3002"

# Better Auth client – base URL for API requests (client-side; baked at build time)
# On Vercel set to same as BETTER_AUTH_BASE_URL, e.g. https://your-app.vercel.app
# Optional locally: defaults to current origin in browser
VITE_BETTER_AUTH_BASE_URL="http://localhost:3002"

# Firecrawl – required for scrape/import features
# Get a key at https://firecrawl.dev
FIRECRAWL_API_KEY="fc-..."

# OpenRouter – for AI summary and other AI features
# Get a key at https://openrouter.ai
AI_KEY="sk-or-..."
